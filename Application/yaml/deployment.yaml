kind: Deployment
apiVersion: apps/v1
metadata:
# Set your lab user name
  name: tzach112-hello-world
  namespace: tzach112-application
spec:
  replicas: 3
  selector:
    matchLabels:
# Set your lab user name
      app: tzach112-hello-world
  template:
    metadata:
      labels:
# Set your lab user name
        app: tzach112-hello-world
    spec:
      containers:
        - resources: {}
          terminationMessagePath: /dev/termination-log
          name: hello-world
          ports:
            - containerPort: 8080
              protocol: TCP
          imagePullPolicy: IfNotPresent
# Update with the image you build in part 1
          image: 'quay.io/tzach112/ostesttsach:v1'
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 25%
      maxSurge: 25%
  revisionHistoryLimit: 10